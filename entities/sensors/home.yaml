platform: template
sensors:
  home:
    friendly_name: Home
    icon: mdi:home
    value_template: "{%-  if is_state('person.jorge','home') or is_state('person.marlyn','home') -%}occupied{%- else -%}empty{%- endif -%}"
