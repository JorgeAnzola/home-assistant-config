---
platform: template
sensors:
  low_temperature_1d:
    friendly_name: "Low Temperature 1D"
    value_template: >
      {% set forecast = state_attr("weather.buienradar", "forecast")[0] %}
      {{ forecast.templow | round }}°C
