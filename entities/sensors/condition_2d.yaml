---
platform: template
sensors:
  condition_2d:
    friendly_name: "Condition 2D"
    value_template: >
      {% set forecast = state_attr("weather.buienradar", "forecast")[1] %}
      {{ as_timestamp(forecast.datetime) | timestamp_custom('%a') }}, {{ forecast.condition | title }}
