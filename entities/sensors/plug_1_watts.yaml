---
platform: template
sensors:
  plug_1_watts:
    friendly_name_template: "{{ states.switch.plug_1.name}} Current Consumption"
    value_template: '{{ states.switch.plug_1.attributes["current_power_w"] | float }}'
    unit_of_measurement: "W"
