---
recorder:
  db_url: !secret db_url
  purge_keep_days: 14
  exclude:
    domains:
      - automation
      - updater
    entities:
      - sun.sun
      - sensor.last_boot
      - sensor.date
    event_types:
      - call_service